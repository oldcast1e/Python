# Your code goes here. Define a function called 'sign'
# 아래 셀에서 숫자 인수를 사용하여 음수이면 -1, 양수이면 1, 0이면 0을 반환하는 부호라는 함수를 정의합니다.funsign
# 아래 셀에서 숫자 인수를 사용하여 음수이면 -1, 양수이면 1, 0이면 0을 반환하는 부호라는 함수를 정의합니다.
def sign(n):
    if n > 0 : return(1)
    if(n == 0): return (0)
    else :return -1
    # els
# Check your answer
